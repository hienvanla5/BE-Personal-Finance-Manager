spring:
  application:
    name: personal-finance-manager
  datasource:
    url: jdbc:mysql://localhost:3306/pfm_db?useSSL=false&serverTimezone=UTC
    username: root
    password: 29012005
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

